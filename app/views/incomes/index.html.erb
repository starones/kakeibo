<h1>収入科目一覧</h1>

<%= link_to "収入科目の新規登録", new_income_path %>
<% if @incomes.present? %>
  <table>
    <thead>
      <tr>
        <th>科目名</th>
        <th>備考</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @incomes.each do |income| %>
        <tr>
          <td><%= income.name %></td>
          <td><%= income.description %></td>
          <td>
            <%= link_to "編集", edit_income_path(income) %>
            <%= link_to "削除", income_path(income), method: :delete, "data-confirm" => "本当に削除します？" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <h3>登録されている収入科目はありません。</h3>
<% end %>