<h2>変動費科目一覧</h2>

<%= link_to "変動費科目の新規登録", new_variablecost_path %>

<% if @variablecosts.present? %>
  <table>
    <thead>
      <tr>
        <th>科目名</th>
        <th>備考</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @variablecosts.each do |variablecost| %>
        <tr>
          <td><%= variablecost.name %></td>
          <td><%= variablecost.description %></td>
          <td>
            <%= link_to "編集", edit_variablecost_path(variablecost) %>
            <%= link_to "削除", variablecost_path(variablecost), method: :delete, "data-confirm" => "本当に削除します？" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <% else %>
    <h3>登録されている変動費科目がありません。</h3>
  <% end %>