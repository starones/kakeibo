<h2>固定費科目一覧</h2>

<%= link_to "固定費科目の新規登録", new_fixedcost_path %>

<% if @fixedcosts.present? %>
  <table>
    <thead>
      <tr>
        <th>科目名</th>
        <th>備考</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @fixedcosts.each do |fixedcost| %>
        <tr>
          <td><%= fixedcost.name %></td>
          <td><%= fixedcost.description %></td>
          <td>
            <%= link_to "編集", edit_fixedcost_path(fixedcost) %>
            <%= link_to "削除", fixedcost_path(fixedcost), method: :delete, "data-confirm" => "本当に削除します？" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <% else %>
    <h2>登録されている固定費科目がありません。</h2>
  <% end %>
